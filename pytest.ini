[pytest]
testpaths = tests
markers =
    browser: marks tests as requiring a browser (deselect with '-m "not browser"')
    visual: marks tests as visual regression tests (deselect with '-m "not visual"')
    slow: marks tests as slow running
addopts = -v --tb=short
